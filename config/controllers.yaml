controller_manager:
  ros__parameters:
    update_rate: 50   # Hz — must not exceed gazebo physics rate (tracker_world: 0.02 s step = 50 Hz)

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    joint_group_velocity_controller:
      type: velocity_controllers/JointGroupVelocityController

joint_group_velocity_controller:
  ros__parameters:
    joints:
      - pan_joint
      - tilt_joint
    command_interfaces:
      - velocity
    state_interfaces:
      - position
      - velocity
