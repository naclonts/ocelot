controller_manager:
  ros__parameters:
    update_rate: 100  # Hz — matches physics step (1 ms = 1000 Hz max, 100 is safe)

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    joint_group_velocity_controller:
      type: velocity_controllers/JointGroupVelocityController

joint_group_velocity_controller:
  ros__parameters:
    joints:
      - pan_joint
      - tilt_joint
    command_interfaces:
      - velocity
    state_interfaces:
      - position
      - velocity
